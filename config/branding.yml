app:
  name: "Naxeu"
  tagline: "Dein Geld im Überblick."
  englishTagline: "AI-assisted finance overview for families."
  # Files live in the repo `branding/` folder; the API serves them at /brand-assets/<filename>.
  # Legacy paths beginning with /branding/ are rewritten to /brand-assets/ in the PWA.
  logo: "/brand-assets/naxeulogowhite.svg"
  favicon: "/brand-assets/favicon.ico"

theme:
  # material/design.md — Naxeu palette (sync with apps/web/src/plugins/vuetify.ts)
  primaryColor: "#102A1C"
  accentColor: "#B8F36B"
  darkModeDefault: true

domains:
  website: "https://naxeu.com"
  app: "https://naxeu.app"
  cloud: "https://naxeu.cloud"

email:
  fromName: "Naxeu"
  fromAddress: "noreply@naxeu.com"

legal:
  imprintUrl: ""
  privacyUrl: ""
